# Run Manifest

## Objective And Fidelity

This serious-route bundle answers whether the supplied evidence supports a global
October rollout. It uses low acceptable loss and preserves regional scope, the EU
blocking condition, contradictory readiness language, source families, and derivation.
All source statements remain source-only.

## Source Order

Sources were read and registered in the declared order:

1. `S4` (`S4-R1`, family `F4`)
2. `S3` (`S3-R1`, family `F3`)
3. `S2` (`S2-R1`, family `F1`, derived entirely from `S1`)
4. `S1` (`S1-R1`, family `F1`)

Each one-line plain-text source is represented by one complete line chunk. The copied
inputs, source hashes, line counts, and native line locators bind every evidence row to
the supplied bytes.

## Composition Rule

`S2` is retained as contrary source text because it says the October rollout is ready,
but it is excluded as independent corroboration. Both the registry and `S2` itself say
that it derives entirely from `S1`, and both share source family `F1`.

## Order Check

A reverse-order stance check produced the same result and confidence: regional support
from `S1` and `S3` does not establish global readiness, `S4` supplies an explicit
Europe-related blocker, and `S2` adds no independent source family.
