# Run Manifest

This schema-v3 serious run answers the launch-gate probe from `S1-R1` while preserving
the required two-chunk boundary plan. Fidelity is low-loss: the line 2 overlap is loaded
into the second chunk to complete the proposition with line 3, but it is not treated as
a second supporting observation.

Completion requires one active claim, one canonical supporting evidence record at lines
2-3, full unique-line coverage, and passing boundary, overlap, and representation checks.
All fixture assertions remain source-only.
