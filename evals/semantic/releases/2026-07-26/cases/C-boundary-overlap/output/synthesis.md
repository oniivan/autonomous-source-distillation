# Synthesis

## Answer

Launch remains blocked until the backup restore drill passes. The exact gating
condition is therefore **the backup restore drill passing**. [C1; E1; S1-R1 lines 2-3]

## Boundary Handling

The proposition begins on line 2 at the end of `S1-R1-C001` and completes on line 3.
`S1-R1-C002` loads line 2 as overlap and adds lines 3-4 as unique content, allowing the
complete proposition to be extracted as canonical evidence `E1`. It is represented once
and gives claim `C1` exactly one supporting observation. Line 4 is retained as context
in `E2`, but it does not independently restate or support the condition.

The answer remains source-only because no external verification was requested or
performed.
