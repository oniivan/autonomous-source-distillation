# Synthesis

Launch is gated on the backup restore drill passing. It remains blocked until that
condition is met (`C1`, supported once by canonical evidence `E1` at `S1-R1` lines
2-3).

Line 2 is loaded in both chunks to preserve the boundary-spanning proposition, but it
is overlap in `S1-R1-C002`, not a second support observation. The statement remains
source-only because no external verification was requested.
