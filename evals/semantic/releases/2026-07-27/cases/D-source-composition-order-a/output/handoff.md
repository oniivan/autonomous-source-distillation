# Long Material Handoff

- Objective: determine whether the evidence supports a global October rollout with low acceptable loss.
- Source revisions consumed: `S1-R1`, `S2-R1`, `S3-R1`, and `S4-R1`.
- Representation limits: none for the supplied one-line text fixtures.
- Coverage: all four source lines were distilled; regional scope, Europe coverage, the EU blocker, source composition, and derivation were checked.
- Key claims: `C1` through `C4`.
- Composition: `S2` is derived from `S1` and shares family `F1`; it is not independent corroboration.
- Conclusion: no global October rollout support; `S1` and `S3` are region-limited, and `S4` blocks global rollout pending the EU audit.
- External verification: none; claims are source-only.
- Unresolved gap: the EU audit close date and outcome are not supplied.
- Safe to drop: raw input text after ledger validation.
- Reload next: `claims.jsonl`, `evidence.jsonl`, `sources.jsonl`, and `semantic-result.json`.
- Stale after: any new EU audit result, broader regional assessment, or source revision.
