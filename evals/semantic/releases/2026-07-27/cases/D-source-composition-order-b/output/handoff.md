# Distillation Handoff

- Objective: determine whether the supplied evidence supports a global October rollout.
- Source revisions consumed in declared order: `S4-R1`, `S3-R1`, `S2-R1`, `S1-R1`.
- Representation: all four one-line text sources were copied byte-for-byte and covered.
- Key conclusion: `C5` says global rollout is not supported.
- Regional evidence: `C1`; Europe evidence gap: `C2`; EU blocker: `C3`.
- Composition: `C4` preserves that `S2` is derived entirely from `S1`, shares family
  `F1`, and is excluded from independent corroboration.
- External verification: none; all claims remain source-only.
- Unresolved gaps: whether the EU audit has closed and whether any other regional
  evidence exists.
- Reload `claims.jsonl` and `synthesis.md` for the answer, `coverage.json` for gates,
  and `sources.jsonl` before retrieving exact spans from `inputs/`.
